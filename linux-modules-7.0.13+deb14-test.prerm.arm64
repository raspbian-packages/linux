#!/bin/sh -e

version=7.0.13+deb14-test

if [ "$1" = remove ]; then
  find "/usr/lib/modules/${version}" \
    -maxdepth 1 \
    -type f \
    -name "modules.*" \
    -not -name "modules.builtin" \
    -not -name "modules.builtin.modinfo" \
    -not -name "modules.order" \
    -delete
fi

exit 0
