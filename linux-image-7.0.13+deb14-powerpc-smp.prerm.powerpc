#!/bin/sh -e

version=7.0.13+deb14-powerpc-smp
image_path=/boot/vmlinux-$version

if [ "$1" != remove ]; then
    exit 0
fi

linux-check-removal $version

linux-run-hooks image prerm $version $image_path -- "$@"

exit 0
